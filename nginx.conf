http {
    server {
        listen 80;
        server_name _;

        location /health {
            return 200 "Healthy";
        }

        # If your bot needs to handle webhooks in the future
        # location /bot {
        #     proxy_pass http://localhost:8080;  # Assuming your bot listens here for webhooks
        #     ...
        # }
    }
}
